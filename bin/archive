#!/bin/sh

. ../lib/archive.sh

main() {
  option="$1"
  argument="$2"

  case "${option}" in
    -a | --add) add "${argument}" 0 ;;
    -m | --move) add "${argument}" 1 ;;
    -l | --list) list ;;
    -h | --help) help ;;
    -v | --version) usage ;;
    *) help ;;
  esac
}

main "$@"
